<UserControl x:Class="RoleSystemDemo.Views.BlogEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:RoleSystemDemo.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance vm:BlogEditorViewModel}"
             mc:Ignorable="d">
    <Grid MaxWidth="800" Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 页面标题 -->
        <TextBlock Grid.Row="0"
                   Margin="0,0,0,20"
                   Text="{Binding PageTitle}"
                   Style="{StaticResource TitleTextBlockStyle}" />

        <!-- 标题输入 -->
        <TextBlock Grid.Row="1"
                   Margin="0,0,0,6"
                   Text="标题"
                   FontWeight="Medium" />
        <TextBox Grid.Row="2" Margin="0,0,0,16" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />

        <!-- 正文输入 -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                       Margin="0,0,0,6"
                       Text="正文"
                       FontWeight="Medium" />
            <TextBox Grid.Row="1"
                     MinHeight="200"
                     Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />
        </Grid>

        <!-- 错误提示 -->
        <TextBlock Grid.Row="4"
                   Margin="0,8,0,0"
                   Text="{Binding ErrorMessage}"
                   Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />

        <!-- 操作按钮 -->
        <StackPanel Grid.Row="5"
                    Margin="0,16,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Margin="0,0,10,0"
                    Padding="20,8"
                    Content="取消"
                    Command="{Binding CancelCommand}" />
            <Button Padding="20,8"
                    Content="保存"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource AccentButtonStyle}" />
        </StackPanel>
    </Grid>
</UserControl>
